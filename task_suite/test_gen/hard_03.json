{
  "task_id": "test_gen_hard_03",
  "dimension": "test_gen",
  "difficulty": "hard",
  "language": "python",
  "code_source": "ground_truth",
  "prompt": "以下是一个简单的用户认证模块。请写完整的 pytest 测试套件，覆盖：成功注册、重复注册、成功登录、密码错误登录、用户不存在登录。使用 fixtures 做 setup/teardown。测试放在 ```python 代码块中。\n\n模块:\n```python\nimport hashlib\n\nclass AuthService:\n    def __init__(self):\n        self.users = {}\n\n    def register(self, username, password):\n        if username in self.users:\n            raise ValueError('用户已存在')\n        if len(password) < 6:\n            raise ValueError('密码至少6位')\n        self.users[username] = hashlib.sha256(password.encode()).hexdigest()\n        return True\n\n    def login(self, username, password):\n        if username not in self.users:\n            return False\n        hashed = hashlib.sha256(password.encode()).hexdigest()\n        return self.users[username] == hashed\n```",
  "ground_truth_code": "import hashlib\n\nclass AuthService:\n    def __init__(self):\n        self.users = {}\n\n    def register(self, username, password):\n        if username in self.users:\n            raise ValueError('用户已存在')\n        if len(password) < 6:\n            raise ValueError('密码至少6位')\n        self.users[username] = hashlib.sha256(password.encode()).hexdigest()\n        return True\n\n    def login(self, username, password):\n        if username not in self.users:\n            return False\n        hashed = hashlib.sha256(password.encode()).hexdigest()\n        return self.users[username] == hashed\n",
  "judge": {"method": "sandbox"}
}
